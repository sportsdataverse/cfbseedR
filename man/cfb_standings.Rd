% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standings.R
\name{cfb_standings}
\alias{cfb_standings}
\title{Compute College Football Standings}
\usage{
cfb_standings(
  games,
  teams,
  ...,
  tiebreaker_depth = c("SOS", "PRE-SOV", "POINTS", "RANDOM"),
  playoff_seeds = NULL,
  rankings = NULL,
  verbosity = c("MIN", "MAX", "NONE")
)
}
\arguments{
\item{games}{A data frame of games. Required columns:
\describe{
\item{sim or season}{A season or simulation ID.}
\item{game_type}{One of \code{"REG"}, \code{"CONF_CHAMP"}, \code{"POST"}.}
\item{week}{Week number of the game.}
\item{home_team, away_team}{Team names matching \code{teams$team}.}
\item{result}{Home margin, i.e. home score minus away score. Must not
be \code{NA} (play or simulate the games first).}
}}

\item{teams}{A data frame with columns \code{team} and \code{conference}. Teams with
conference \code{"FBS Independents"} or \code{NA} are treated as independents:
they appear in overall standings but receive no conference rank.}

\item{...}{Currently unused.}

\item{tiebreaker_depth}{One of \code{"SOS"} (default), \code{"PRE-SOV"}, \code{"POINTS"},
or \code{"RANDOM"}. Controls how deep the tiebreaker cascade goes before
falling back to a coin flip:
\itemize{
\item \code{"RANDOM"}: coin flip immediately.
\item \code{"PRE-SOV"}: head-to-head and common opponents only.
\item \code{"SOS"}: adds strength of victory, then strength of schedule.
\item \code{"POINTS"}: adds conference point differential.
}}

\item{playoff_seeds}{If not \code{NULL}, a \code{seed} column is added via
\code{\link[=cfb_playoff_seeds]{cfb_playoff_seeds()}} with this number of playoff spots.}

\item{rankings}{Optional committee-style rankings data frame with columns
\code{team} and \code{rank}, passed to \code{\link[=cfb_playoff_seeds]{cfb_playoff_seeds()}}. Ignored when
\code{playoff_seeds} is \code{NULL}.}

\item{verbosity}{One of \code{"MIN"} (default), \code{"MAX"}, or \code{"NONE"}.
\code{"MAX"} logs every tiebreaker step.}
}
\value{
A tibble of standings, one row per (\code{sim}, \code{team}) (the id column
is named \code{season} if the input used \code{season}), sorted by sim,
conference, conference rank, and team. Columns: \code{sim}/\code{season}, \code{team},
\code{conference}, \code{games}, \code{wins}, \code{losses}, \code{ties}, \code{win_pct}, \code{pd},
\code{conf_games}, \code{conf_wins}, \code{conf_losses}, \code{conf_ties}, \code{conf_pct},
\code{conf_pd}, \code{sov}, \code{sos}, \code{conf_rank}, \code{conf_champ} (+ \code{seed} if
\code{playoff_seeds} was supplied). \code{wins}/\code{losses} are true win/loss counts;
\code{win_pct} counts ties as half a win. \code{sov}/\code{sos} are conference-scoped.
}
\description{
Computes overall and conference standings from a table of game results,
including conference ranks (via a documented tiebreaker cascade),
conference champions, and - optionally - College Football Playoff seeds.

Adapted from \href{https://nflseedr.com}{nflseedR}'s \code{nfl_standings()} with
college football semantics: conferences instead of divisions, independents
excluded from conference ranks, and conference championship games counting
toward the overall record (and deciding the champion) but not the
conference record.
}
\details{
The tiebreaker cascade is a documented simplification - real CFB
tiebreakers are conference-specific. Conference ranks are seeded by
conference win percentage; ties are broken by head-to-head record among
the tied teams, record vs. common conference opponents (minimum one),
conference-scoped strength of victory, conference-scoped strength of
schedule, conference point differential, and finally a coin flip. All
cascade quantities are computed over regular-season conference games so
conference ranks depend only on conference play.
}
\examples{
games <- read.csv(system.file("extdata", "toy_games.csv", package = "cfbseedR"))
teams <- read.csv(system.file("extdata", "toy_teams.csv", package = "cfbseedR"))
standings <- cfb_standings(games, teams, tiebreaker_depth = "POINTS",
                           verbosity = "NONE")
standings[, c("team", "conference", "conf_rank", "conf_champ")]

}
\seealso{
\code{\link[=cfb_playoff_seeds]{cfb_playoff_seeds()}}, \code{\link[=cfb_simulations]{cfb_simulations()}},
the nflseedR original: \url{https://nflseedr.com}
}
