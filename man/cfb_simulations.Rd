% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulations.R
\name{cfb_simulations}
\alias{cfb_simulations}
\title{Simulate a College Football Season}
\usage{
cfb_simulations(
  games,
  teams,
  compute_results = cfbseedR_compute_results,
  ...,
  simulations = 10000L,
  playoff_seeds = 12L,
  tiebreaker_depth = c("SOS", "PRE-SOV", "POINTS", "RANDOM"),
  sim_include = c("POST", "REG"),
  rankings = NULL
)
}
\arguments{
\item{games}{A data frame of games of a \strong{single} season, in the schema
of \code{\link[=cfb_standings]{cfb_standings()}} plus an optional \code{neutral} column (0/1). Games
with \code{result = NA} are simulated. Must not contain
\code{game_type == "POST"} rows - the playoff bracket is generated from the
computed seeds.}

\item{teams}{A data frame with columns \code{team} and \code{conference}. Teams with
conference \code{"FBS Independents"} or \code{NA} are treated as independents:
they appear in overall standings but receive no conference rank.}

\item{compute_results}{A function computing results of games, with
the required arguments \code{teams}, \code{games}, and \code{week_num}. See
\code{\link[=simulations_verify_fct]{simulations_verify_fct()}} for the contract and
\code{\link[=cfbseedR_compute_results]{cfbseedR_compute_results()}} for the default.}

\item{...}{Additional parameters passed on to \code{compute_results}.}

\item{simulations}{The number of times the season shall be simulated.}

\item{playoff_seeds}{Number of CFP spots (default 12), passed to
\code{\link[=cfb_playoff_seeds]{cfb_playoff_seeds()}}.}

\item{tiebreaker_depth}{One of \code{"SOS"} (default), \code{"PRE-SOV"}, \code{"POINTS"},
or \code{"RANDOM"}. Controls how deep the tiebreaker cascade goes before
falling back to a coin flip:
\itemize{
\item \code{"RANDOM"}: coin flip immediately.
\item \code{"PRE-SOV"}: head-to-head and common opponents only.
\item \code{"SOS"}: adds strength of victory, then strength of schedule.
\item \code{"POINTS"}: adds conference point differential.
}}

\item{sim_include}{One of \code{"POST"} (default) or \code{"REG"}:
\itemize{
\item \code{"REG"}: simulate the remaining schedule and compute standings,
conference champions, and playoff seeds.
\item \code{"POST"}: \code{"REG"} + simulate the playoff bracket.
}}

\item{rankings}{Optional committee rankings (\code{team}, \code{rank}) used for CFP
seeding, held static across simulations. When \code{NULL}, seeding falls
back to the per-simulation standings ordering (see
\code{\link[=cfb_playoff_seeds]{cfb_playoff_seeds()}}).}
}
\value{
A list of class \code{cfbseedR_simulation} with these elements:\tabular{lll}{
   Element \tab Type \tab Description \cr
   \code{standings} \tab tibble \tab Per-simulation standings in the \code{\link[=cfb_standings]{cfb_standings()}} schema (see its column table; \code{sov}/\code{sos} are conference-REG-scoped) plus \code{seed} and - with \code{sim_include = "POST"} - \code{exit} (integer: 0 = missed playoff, r = eliminated in round r, max round + 1 = national champion). \cr
   \code{games} \tab tibble \tab All games of all simulations (\code{sim}, \code{game_type}, \code{week}, \code{home_team}, \code{away_team}, \code{result}, \code{neutral}), incl. generated playoff games. \cr
   \code{overall} \tab tibble \tab Per-team means across simulations: \code{wins} (average), and probabilities \code{conf_champ}, \code{playoff}, \code{seed1}, \code{won_natty}. \cr
   \code{team_wins} \tab tibble \tab Per-team probability of clearing each half-win threshold (\code{team}, \code{wins}, \code{over_prob}, \code{under_prob}). \cr
   \code{game_summary} \tab tibble \tab Per-matchup aggregates: \code{away_wins}, \code{home_wins}, \code{ties}, mean \code{result}, \code{games_played}, \code{away_percentage}, \code{home_percentage}. \cr
   \code{sim_params} \tab list \tab The simulation parameters (number of simulations, seeds, depth, etc.). \cr
}
}
\description{
Simulates college football seasons based on a games/schedule table that
holds matchups with and without results. Missing results are computed
week by week using the pluggable \code{compute_results} function (default:
\code{\link[=cfbseedR_compute_results]{cfbseedR_compute_results()}}, an ELO-based generator adapted from
nflseedR). After the scheduled games, standings, conference champions,
and CFP seeds are computed, and - with \code{sim_include = "POST"} - the
playoff bracket is simulated round by round.
}
\details{
The playoff bracket is a standard single-elimination bracket of size
\code{2^ceiling(log2(playoff_seeds))} with byes for the top seeds - for 12
seeds this reproduces the CFP bracket (quarterfinals 1 vs 8/9 winner,
4 vs 5/12, 3 vs 6/11, 2 vs 7/10). First-round games are hosted by the
higher seed; later rounds are neutral-site. There is no reseeding
(fixed bracket, per the CFP format). Conference championship matchups
are simulated as scheduled, not re-derived from simulated standings.

Simulations run sequentially (no chunk/parallel support). Set a seed
with \code{set.seed()} for reproducibility.
}
\examples{
\donttest{
games <- read.csv(system.file("extdata", "toy_games.csv", package = "cfbseedR"))
teams <- read.csv(system.file("extdata", "toy_teams.csv", package = "cfbseedR"))
games$result[games$week >= 3] <- NA
set.seed(4)
sim <- cfb_simulations(games, teams, simulations = 4, playoff_seeds = 4)
sim$overall
}

}
\seealso{
\code{\link[=cfb_standings]{cfb_standings()}}, \code{\link[=cfb_playoff_seeds]{cfb_playoff_seeds()}},
\code{\link[=cfbseedR_compute_results]{cfbseedR_compute_results()}}, \code{\link[=simulations_verify_fct]{simulations_verify_fct()}},
the nflseedR original: \url{https://nflseedr.com}
}
