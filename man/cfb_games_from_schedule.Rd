% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/games_from_schedule.R
\name{cfb_games_from_schedule}
\alias{cfb_games_from_schedule}
\title{Build an Engine Games Table from a cfbfastR Schedule}
\usage{
cfb_games_from_schedule(schedule)
}
\arguments{
\item{schedule}{A data frame with at least \code{season}, \code{week},
\code{season_type}, \code{home_team}, \code{away_team}. Optional columns used when
present: \code{home_points}/\code{away_points} (to compute \code{result}),
\code{neutral_site}, and \code{notes} (games whose notes mention "championship"
are classified as \code{"CONF_CHAMP"}).}
}
\value{
A tibble in the engine games schema, ready for
\code{\link[=cfb_standings]{cfb_standings()}} / \code{\link[=cfb_simulations]{cfb_simulations()}}:\tabular{lll}{
   Column \tab Type \tab Description \cr
   \code{season} \tab integer \tab Season taken from the schedule. \cr
   \code{week} \tab integer \tab Week number of the game. \cr
   \code{game_type} \tab character \tab \code{"REG"}, \code{"CONF_CHAMP"} (notes mention "championship"), or \code{"POST"} (\code{season_type == "postseason"}). \cr
   \code{home_team} \tab character \tab Home team name. \cr
   \code{away_team} \tab character \tab Away team name. \cr
   \code{result} \tab numeric \tab Home margin (home minus away points); \code{NA} for unplayed games. \cr
   \code{neutral} \tab integer \tab Neutral-site flag (0/1). \cr
}
}
\description{
Maps the schedule shape returned by \code{cfbfastR::load_cfb_schedules()} to
the games schema used by \code{\link[=cfb_standings]{cfb_standings()}} and \code{\link[=cfb_simulations]{cfb_simulations()}}.
cfbfastR is not required - any data frame with the expected columns works.
}
\details{
Game type mapping: \code{season_type == "postseason"} becomes \code{"POST"}, games
whose \code{notes} contain "championship" (case-insensitive) become
\code{"CONF_CHAMP"}, everything else \code{"REG"}. This is a documented heuristic -
CFBD marks conference championship games as regular-season games with a
championship note.
}
\examples{
schedule <- data.frame(
  season = 2024, week = c(1, 1, 15, 16),
  season_type = c("regular", "regular", "regular", "postseason"),
  home_team = c("A1", "B1", "A1", "A1"),
  away_team = c("A2", "B2", "A2", "B1"),
  home_points = c(21, 24, 17, NA), away_points = c(14, 20, 14, NA),
  neutral_site = c(FALSE, FALSE, TRUE, TRUE),
  notes = c(NA, NA, "Alpha Championship Game", NA)
)
cfb_games_from_schedule(schedule)

}
\seealso{
\code{\link[=cfb_standings]{cfb_standings()}}, \code{\link[=cfb_simulations]{cfb_simulations()}},
\code{cfbfastR::load_cfb_schedules()} from
\href{https://cfbfastR.sportsdataverse.org}{cfbfastR},
the nflseedR original: \url{https://nflseedr.com}
}
