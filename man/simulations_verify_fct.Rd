% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulations_verify_fct.R
\name{simulations_verify_fct}
\alias{simulations_verify_fct}
\title{Verify a Custom CFB Result Simulation Function}
\usage{
simulations_verify_fct(compute_results, ..., games = NULL, teams = NULL)
}
\arguments{
\item{compute_results}{A function to compute results of games. Required
arguments: \code{teams}, \code{games}, \code{week_num}. It must return
\code{list(teams = teams, games = games)} without removing rows or columns
from \code{games}, must fill \code{result} for exactly the games with
\code{week == week_num & is.na(result)}, must not modify any other result,
and must not produce ties (\code{result == 0}) outside the regular season.}

\item{...}{Further arguments passed on to \code{compute_results}.}

\item{games}{A schedule where some results are missing. Defaults to the
bundled toy season with all results from week 3 onwards blanked.}

\item{teams}{A teams table (\code{team}, \code{conference}, optionally \code{sim}).
Defaults to the bundled toy teams.}
}
\value{
Returns \code{TRUE} invisibly if no problems are found.
}
\description{
cfbseedR supports custom functions to compute results in season
simulations through the \code{compute_results} argument of
\code{\link[=cfb_simulations]{cfb_simulations()}}. This function verifies that a custom function
behaves as the simulator expects, mirroring nflseedR's
\code{simulations_verify_fct()}. It checks the output structure and whether
game results are changed as expected, and errors with a hint at the
first problem found.
}
\examples{
\donttest{
simulations_verify_fct(cfbseedR_compute_results)
}

}
\seealso{
\code{\link[=cfb_simulations]{cfb_simulations()}}, \code{\link[=cfbseedR_compute_results]{cfbseedR_compute_results()}}
}
